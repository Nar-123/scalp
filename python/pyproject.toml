[project]
name = "scalp-python"
version = "0.2.0"
description = "Local analytics + self-learning foundation for the New-Token Ultra Scalper V1 (Phase 2: no AI/LLM integration yet)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
